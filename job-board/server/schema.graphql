type Query {
  job: Job
  jobs: [Job!]
}

"""
Represents a job ad posted to our board
"""
type Job {
  """
  객체의 ID
  """
  id: ID!
  date: String!
  title: String!
  description: String
  company: Company!
}

type Company {
  id: ID!
  name: String!
  description: String
}
